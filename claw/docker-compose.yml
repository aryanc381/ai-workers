services:
  claw:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "18789:18789"
    volumes:
      - openclaw-state:/home/node/.openclaw
    command: ["--allow-unconfigured"]

volumes:
  openclaw-state:
